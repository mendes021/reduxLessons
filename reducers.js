// someReducer.js
const initialState = /* your initial state */0;

const someReducer = (state = initialState, action) => {
  // reducer logic
  return state; // replace with your actual logic
};

module.exports = someReducer;
